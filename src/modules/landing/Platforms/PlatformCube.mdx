import { Canvas, Meta } from '@storybook/blocks';

import * as PlatformCubeStories from './PlatformCube.stories';

<Meta of={PlatformCubeStories} />

# PlatformCube

The PlatformCube is a compact way to represent the interactions between the four platforms in Graasp.


<Canvas of={PlatformCubeStories.Default}/>
